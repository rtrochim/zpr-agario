To start this app just run ./build.sh

To run tests cd to src/tests/ and run 'make'.

This app needs
- libsqlite3-dev
    Should be preinstalled on Ubuntu since 18.04, if not run 'sudo apt-get install libsqlite3-dev'
- cmake (min 3.3)

For tests:
- boost
- lcov